import {StyleSheet} from 'react-native';
import {sh} from '../../constants/theme';

const createStyles = () =>
  StyleSheet.create({
    propMarketText: {
      marginBottom: sh(12)
    },
   
  });

export default createStyles;
